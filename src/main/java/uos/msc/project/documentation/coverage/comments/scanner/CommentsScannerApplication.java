package uos.msc.project.documentation.coverage.comments.scanner;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class CommentsScannerApplication {

	/**
	 * Main method to run the spring boot application
	 * @param args arguments to pass to
	 * the SpringBootApplication on startup.
	 */
	public static void main(String[] args) {
		SpringApplication.run(CommentsScannerApplication.class, args);
	}

}
